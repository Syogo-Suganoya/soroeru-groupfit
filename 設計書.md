# ソロエル 設計書

グループ集合試着エージェント（CANDIDATE 12）

## 1. 概要

結婚式のお呼ばれ仲間・成人式の同級生・コスプレの合わせなど、グループ全員のバーチャル試着を1枚の「集合プレビュー」に合成し、衣装かぶり・浮きを事前検知するエージェント。

- **一言価値**: 「並んだときの見え方」を当日ではなく事前に確認できる、世界初のグループ試着体験
- **テーマ適合**: 「他人の顔をグループ内で扱う」という繊細な問題に、同意ベースの合成という設計答案を示す

## 2. 課題

| 課題 | 現状 |
|---|---|
| 衣装かぶり事故 | ドレスの色かぶり・振袖の柄かぶりは当日会場で判明。事前手段はグループLINEの自己申告のみ |
| 1人だけ浮く不安 | フォーマル度・色味の目線合わせは口頭では揃わない |
| 既存試着は1人用 | グループで並んだ見え方を合成・検証するサービスは存在しない（2026-08 検索確認済み） |

## 3. ユースケース

1. **結婚式お呼ばれ**: ドレスコード制約（白NG等）込みでグループの配色を最適化
2. **成人式**: 同級生グループの振袖の柄・色かぶり検知
3. **コスプレ合わせ**: 同作品キャラの並び確認（コスめぐりへの機能提供も可能）
4. **事業者**: レンタル事業者の「グループ送客」チャネル

## 4. システムアーキテクチャ

![技術スタック](docs/architecture.png)

（図の生成: `docker compose --profile diagram run --rm diagram` / 定義は `docs/architecture.py`）

```
[PWA（幹事がルーム作成→URLで招待）]
      │
      ▼
[API Gateway (Cloud Run: api)]
      │
      ▼
[Orchestrator Agent (ADK / Cloud Run: agent)]
  ├── 個人試着エージェント ──── YouCam API (Clothes Try-On, Facial Color Tones)
  ├── 合成エージェント ──────── 集合プレビュー生成（同意済みメンバーのみ）
  ├── 調和エージェント ──────── かぶり・浮き検知（色相/柄/フォーマル度）+ Gemini
  └── 手配エージェント ──────── レンタル・購入リンク接続、進捗リマインド
      │
      ▼
[Firestore]（ルーム・同意状態 / イベント後TTL削除）
[Cloud Logging]（同意・削除の証跡）
```

### エージェント構成（ADK）

| エージェント | 役割 | 自律性 |
|---|---|---|
| Orchestrator | ルーム進行の管理、全員確定の検知 | ルーム状態駆動で自律 |
| 個人試着 | 各メンバーの試着画像生成、候補提示 | 提案まで |
| 合成 | 集合プレビューのリアルタイム更新 | **合成は本人同意が前提** |
| 調和 | かぶり検知（色差ΔE・柄類似）、浮き検知（フォーマル度スコア）、代替案提示 | 検知・提案は自律 |
| 手配 | 確定後のリンク接続、準備進捗の見守り・リマインド | リマインドは自律 |

### 調和判定ロジック

- **色かぶり**: 試着衣装の主要色を抽出し、CIEDE2000色差がしきい値未満のペアを警告
- **浮き**: Geminiでフォーマル度を5段階スコア化し、グループ中央値から2以上乖離で警告
- **ドレスコード**: 白・全身黒・ファー等のNGルールをシーン別プリセットで検証
- 判定根拠（色相環・スコア）をUIに常時表示し、AIの美的判断を押し付けない

## 5. 使用API・技術スタック

| 区分 | 技術 |
|---|---|
| 実行基盤（必須要件） | Cloud Run |
| AI（必須要件） | Gemini API（フォーマル度判定・調和提案・ドレスコード解釈）、Agent Development Kit |
| スポンサー | YouCam API（AI Clothes Try-On・Facial Color Tones Analyzer・画像合成系） |
| データ | Firestore、Cloud Storage（ルーム内一時画像） |
| 連携 | アプリ内通知（MVP）、レンタルECリンク。LINE Messaging API はポートのみ用意し将来拡張 |

## 6. データモデル（Firestore）

```
rooms/{roomId}
  event: { 種別, 日時, ドレスコード }
  members: [ { uid, 表示名, consent: {合成同意, 同意日時}, 状態 } ]
  fittings/{memberId}: { 候補衣装[], 確定衣装, 主要色, フォーマル度 }
  preview: { 合成画像ref, 更新履歴 }
  harmony: { 警告[], 根拠 }
  ttl: イベント日 + 7日で全削除
audit/{logId}   # 同意取得・撤回・削除実行の証跡
```

## 7. ガバナンス&セキュリティ設計

1. **同意ベースの合成**: 集合プレビューへの顔合成は明示同意制。未同意メンバーはシルエット表示。同意はいつでも撤回でき、撤回で過去の合成からも即時除去
2. **ルーム単位の完全消去**: 全画像はルームに閉じ、イベント後TTLで全員分を自動削除。エクスポートは本人の分のみ
3. **提案の透明性**: かぶり・浮きの判定根拠（色差値・スコア）を常時開示
4. **可観測性**: 同意の取得・撤回・削除の実行を監査ログに記録し、デモで提示

## 8. 環境構築（Docker）

ローカル開発から本番まで、すべてコンテナで統一する（Cloud Run はコンテナデプロイのため、ローカルと本番で同一イメージを使用できる）。

### 構成

```
.
├── docker-compose.yml        # ローカル開発一式
├── services/
│   ├── api/Dockerfile        # API Gateway（FastAPI）
│   ├── agent/Dockerfile      # ADKエージェント本体
│   └── web/Dockerfile        # フロント（PWA、開発時のみ）
└── .env.example              # APIキーのテンプレート（実キーはコミットしない）
```

### docker-compose サービス

| サービス | 内容 |
|---|---|
| api | API Gateway。ホットリロード有効（ソースをボリュームマウント） |
| agent | ADKエージェント。Gemini API・スポンサーAPIへの接続を担う |
| web | フロント開発サーバー |
| firestore-emulator | Firestoreエミュレータ（gcloud CLIイメージ）。本番データに触れず開発 |

- `docker compose up` で全サービス起動。APIキー類は `.env`（gitignore対象）から注入
- 本番の秘密情報は Secret Manager で管理し、Cloud Run に環境変数として注入（イメージに焼き込まない）

### イメージとデプロイ

- ベースイメージ: `python:3.12-slim`（マルチステージビルドで依存を分離し軽量化）
- デプロイ: `gcloud builds submit` → Cloud Build でビルド → Artifact Registry へ push → Cloud Run へリリース
- ローカル・CI・本番が同一 Dockerfile を参照し、環境差異による不具合を構造的に排除

## 9. MVPスコープ（〜10/15）

| 週 | マイルストーン |
|---|---|
| W1 | ルーム・同意モデル、ADK雛形 |
| W2 | YouCam個人試着パイプライン |
| W3 | 集合プレビュー合成＋リアルタイム更新 |
| W4 | 調和判定（色差・フォーマル度）＋代替提案 |
| W5 | 同意撤回・TTL削除・監査ログ・PWA仕上げ・デプロイ |
| W6 | デモ動画・提出物 |

**MVPで捨てるもの**: 成人式・コスプレのシーンプリセット（結婚式のみ）、実EC連携（リンクのみ）、
外部への通知配信（通知はアプリ内に閉じる。アプリを開かない人に届かない点は割り切る）

## 10. 審査基準との対応

| 基準 | 対応 |
|---|---|
| 課題の新規性 | グループ試着は完全空白（検索立証）。「集合プレビュー」は概念ごと新規 |
| 自律性・エージェントらしさ | ルーム状態駆動の自律進行（検知→提案→リマインド）＋同意管理 |
| 実装品質と拡張性 | シーンプリセット追加で冠婚葬祭全般・制服・チームウェアへ展開可能 |

## 11. 備考

- 単体候補であると同時に、シキめぐり／コスめぐりの「キラー機能」としても移植可能。最終選定でどちらかが選ばれた場合はモジュールとして合流させる選択肢を残す

## 12. 追加案（GMI Cloud 活用）

| 追加機能 | 使用モデル | 内容 |
|---|---|---|
| 集合プレビュー合成の品質エンジン | Seedream / Flux-kontext-pro | 複数人物を1枚に自然に合成する処理を画像編集モデルで実装。ライティング・スケールの整合が製品価値に直結するため効果が大きい |
| 会場ライティング再現 | bria-fibo relight | 「昼のガーデン」「ホテル宴会場」など式場の光環境で集合プレビューを再照明し、当日の見え方に近づける |
| 記念ムービー | image-to-video＋音楽生成 | 確定した集合プレビューから短い記念ムービーを生成し、グループの合意形成を祝う体験に変える |

### 役割分担

- Gemini: 判断・調和提案・会話
- YouCam: 個人の試着・パーソナルカラー
- **GMI Cloud: 重い画像合成・動画生成ワークロード**

### MVPへの影響

- W3の合成実装をGMI（Seedream）ベースに置き換え。relight・ムービーは拡張枠
